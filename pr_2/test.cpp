#include "InterpolationSearch.h"

int main() {
    string fileName = "data.txt";
    string binaryFileName = "binaryData.bin";

    //genegateFile(fileName,100);
    //generateBinaryFile(fileName, binaryFileName);
    LinearSearch(binaryFileName, 399);

    return 0;
}